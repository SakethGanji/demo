fastapi>=0.100.0
python-multipart>=0.0.6
uvicorn>=0.23.0
duckdb>=1.0.0
pandas>=2.0.0
pyarrow>=12.0.0
openpyxl>=3.1.0
jinja2>=3.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
weasyprint>=60.0
httpx>=0.27.0
scikit-learn>=1.3.0

# MCP server (the tool surface mounted at /api/v1/mcp)
mcp>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Object storage (S3/MinIO backend)
boto3>=1.34.0
# Identity (EmailStr validation)
email-validator>=2.0.0

# LLM providers
google-genai>=1.0.0
openai>=1.0.0
anthropic>=0.30.0
json-repair>=0.25.0
docstring-parser>=0.16

# --- Test dependencies ---
# These were installed in the venv but undeclared, so a fresh checkout could not
# run the suite. pytest-asyncio is required by pytest.ini's `asyncio_mode = auto`.
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0    # no coverage baseline existed before 2026-08-06; see HANDOFF-TEST-COVERAGE.md
