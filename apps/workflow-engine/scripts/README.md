# workflow-engine scripts

This directory is reserved for workflow-engine maintenance utilities.

PromptLab Mongo bootstrap (indexes + seed experiment) now lives in
`apps/analytics-service/scripts/seed_promptlab_experiment.py`. Run it as:

    cd apps/analytics-service && python scripts/seed_promptlab_experiment.py
