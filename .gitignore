workflows.db
apps/gemini-cli-main/
apps/tweakcn-main/
__pycache__/
*.pyc
apps/workflow-studio/public/esbuild.wasm
.env
.env.local

# Coverage data is a binary artifact regenerated by every run; it was tracked by
# accident and produced a diff on each suite invocation.
apps/analytics-service/.coverage
apps/analytics-service/htmlcov/
screenshots/

# Playwright run artifacts: traces, videos and the last-run marker. Regenerated
# by every `npm run test:ui`, and `.last-run.json` changes on each invocation.
test-results/
playwright-report/
